# Build outputs
Debug/
Release/
*.o
*.d
*.su
*.map
*.elf
*.list
*.cyclo
*.hex
*.bin

# IDE workspace settings (not project settings)
.settings/
.kilo/

# Visual Studio (VS Code / VS)
.vs/

# Launch files (debug configurations)
*.launch

# Temp files
*~

# AES key (never commit real keys — see key.h.example)
Drivers/Crypto/key.h

# Lyrion Link datasheets (private — kept local or in the private repo)
LYRION_LINK.md
LYRION_LINK_PUBLIC.md