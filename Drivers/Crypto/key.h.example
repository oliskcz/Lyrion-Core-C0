#pragma once
#include <stdint.h>
/* Copy this file to key.h and replace with your own random values.
 * key.h is git-ignored — never commit real keys.
 * The key and nonce prefix must be identical on both the STM32 receiver
 * and the Arduino transmitter. */

static const uint8_t AES_KEY[16] = {
    0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07,
    0x08, 0x09, 0x0A, 0x0B, 0x0C, 0x0D, 0x0E, 0x0F,
};

static const uint8_t AES_NONCE_PREFIX[7] = {
    0xAA, 0xBB, 0xCC, 0xDD, 0xEE, 0xFF, 0x11,
};
